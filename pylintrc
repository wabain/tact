[MESSAGES CONTROL]

good-names = i,j,x,y

disable =
    fixme,
    missing-docstring,
    # handled by black
    format,
    # given to false positives (e.g. multiline subclass method declarations)
    duplicate-code
